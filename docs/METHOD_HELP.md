# FiscalAIClient Method Help

Generated by `tools/generateMethodDocs.m`. Keep method signatures and endpoint notes in that generator.

Default responses use `ReturnType="auto"`. Pass `ReturnType="struct"` for raw JSON-derived data, `ReturnType="table"` for table conversion, or `ReturnType="timetable"` for date-indexed outputs.

## `FiscalAIClient`

- Syntax: `client = fiscalai.FiscalAIClient(ApiKey=..., NormalizeTypes=true)`
- Endpoint: `N/A`
- Returns: A configured `FiscalAIClient` object.
- Notes: API key resolution order is explicit `ApiKey`, MATLAB Vault, environment variable, then `.env`.

## `endpointCatalog`

- Syntax: `catalog = fiscalai.FiscalAIClient.endpointCatalog()`
- Endpoint: `N/A`
- Returns: Table of supported endpoint families, wrappers, and status.
- Notes: Use this to audit wrapper coverage against Fiscal.ai endpoint families.

## `request`

- Syntax: `data = client.request("/v1/path", Query=struct(...))`
- Endpoint: `Any GET path`
- Returns: Auto-converted table/struct data for an arbitrary Fiscal.ai GET endpoint.
- Notes: Use this when Fiscal.ai adds an endpoint before a wrapper is available.

## `companiesList`

- Syntax: `data = client.companiesList(PageNumber=1, PageSize=1000)`
- Endpoint: `/v2/companies-list`
- Returns: Paged company catalog; auto mode returns `data` as a table.
- Notes: Use paging for large catalog reads.

## `companyProfile`

- Syntax: `profile = client.companyProfile(CompanyKey="NASDAQ_MSFT")`
- Endpoint: `/v2/company/profile`
- Returns: Company profile struct by default.
- Notes: Use `ReturnType="struct"` for raw profile fields.

## `companyLogo`

- Syntax: `[bytes, metadata] = client.companyLogo(CompanyKey="NASDAQ_MSFT", Variant="logo")`
- Endpoint: `/v2/company/logo`
- Returns: Image bytes and response metadata; writes to `OutputFile` when supplied.
- Notes: Supported variants include `icon`, `logo`, and `logo-dark`.

## `companyNews`

- Syntax: `news = client.companyNews(CompanyKey="NASDAQ_MSFT", StartDate="2026-01-01")`
- Endpoint: `/v1/company/news`
- Returns: News event data filtered by identifiers and dates.
- Notes: Supports `StartDate`, `EndDate`, `EventType`, and `Importance`.

## `asReportedIncomeStatement`

- Syntax: `data = client.asReportedIncomeStatement(CompanyKey="NASDAQ_MSFT", PeriodType="annual")`
- Endpoint: `/v1/company/financials/income-statement/as-reported`
- Returns: As-reported financial statement data.
- Notes: Accepts standard company identifiers plus `PeriodType` and `Currency`.

## `asReportedBalanceSheet`

- Syntax: `data = client.asReportedBalanceSheet(CompanyKey="NASDAQ_MSFT", PeriodType="annual")`
- Endpoint: `/v1/company/financials/balance-sheet/as-reported`
- Returns: As-reported balance sheet data.
- Notes: Accepts standard company identifiers plus `PeriodType` and `Currency`.

## `asReportedCashFlowStatement`

- Syntax: `data = client.asReportedCashFlowStatement(CompanyKey="NASDAQ_MSFT", PeriodType="annual")`
- Endpoint: `/v1/company/financials/cash-flow-statement/as-reported`
- Returns: As-reported cash flow data.
- Notes: Accepts standard company identifiers plus `PeriodType` and `Currency`.

## `asReportedFinancials`

- Syntax: `data = client.asReportedFinancials("income-statement", CompanyKey="NASDAQ_MSFT")`
- Endpoint: `/v1/company/financials/{statementType}/as-reported`
- Returns: As-reported statement selected by path `statementType`.
- Notes: Supported statement types are `income-statement`, `balance-sheet`, and `cash-flow-statement`.

## `standardizedIncomeStatement`

- Syntax: `data = client.standardizedIncomeStatement(CompanyKey="NASDAQ_MSFT", PeriodType="annual")`
- Endpoint: `/v1/company/financials/income-statement/standardized`
- Returns: Standardized income statement with period rows and metric columns.
- Notes: Enable `NormalizeTypes=true` to convert date-like fields to `datetime`.

## `standardizedBalanceSheet`

- Syntax: `data = client.standardizedBalanceSheet(CompanyKey="NASDAQ_MSFT", PeriodType="annual")`
- Endpoint: `/v1/company/financials/balance-sheet/standardized`
- Returns: Standardized balance sheet with period rows and metric columns.
- Notes: Enable `NormalizeTypes=true` to convert date-like fields to `datetime`.

## `standardizedCashFlowStatement`

- Syntax: `data = client.standardizedCashFlowStatement(CompanyKey="NASDAQ_MSFT", PeriodType="annual")`
- Endpoint: `/v1/company/financials/cash-flow-statement/standardized`
- Returns: Standardized cash flow statement with period rows and metric columns.
- Notes: Enable `NormalizeTypes=true` to convert date-like fields to `datetime`.

## `standardizedFinancials`

- Syntax: `data = client.standardizedFinancials("income-statement", CompanyKey="NASDAQ_MSFT", ReturnType="timetable")`
- Endpoint: `/v1/company/financials/{statementType}/standardized`
- Returns: Standardized statement selected by path `statementType`.
- Notes: Use `ReturnType="timetable"` to receive date-indexed period rows.

## `standardizedMetricsList`

- Syntax: `metrics = client.standardizedMetricsList("standard", "income-statement")`
- Endpoint: `/v1/standardized-metrics-list/{templateType}/{statementType}`
- Returns: Metric catalog for one reporting template and statement.
- Notes: Path values are URL encoded.

## `allStandardizedMetricsList`

- Syntax: `metrics = client.allStandardizedMetricsList()`
- Endpoint: `/v1/standardized-metrics-list`
- Returns: Complete standardized metric catalog.
- Notes: Useful for discovering available metric IDs.

## `ratiosList`

- Syntax: `ratios = client.ratiosList()`
- Endpoint: `/v1/ratios-list`
- Returns: Ratio catalog table.
- Notes: Useful for discovering `RatioId` values.

## `companyRatios`

- Syntax: `data = client.companyRatios(CompanyKey="NASDAQ_MSFT", RatioId="ratio_price_to_earnings")`
- Endpoint: `/v1/company/ratios`
- Returns: Company ratio periods with metric columns in auto mode.
- Notes: Use `RatioId` to request one ratio family.

## `dailyRatios`

- Syntax: `data = client.dailyRatios("ratio_price_to_earnings", CompanyKey="NASDAQ_MSFT")`
- Endpoint: `/v1/company/ratios/daily/{ratioId}`
- Returns: Daily values for one ratio.
- Notes: Pass the ratio ID as the first positional argument.

## `sharesOutstanding`

- Syntax: `data = client.sharesOutstanding(CompanyKey="NASDAQ_MSFT")`
- Endpoint: `/v1/company/shares-outstanding`
- Returns: Shares outstanding payload.
- Notes: Returns the Fiscal.ai payload without endpoint-specific flattening.

## `adjustedMetrics`

- Syntax: `data = client.adjustedMetrics(CompanyKey="NASDAQ_MSFT", PeriodType="annual")`
- Endpoint: `/v1/company/adjusted-metrics`
- Returns: Adjusted metrics payload.
- Notes: Returns adjusted metrics using Fiscal.ai schema.

## `adjustedNumbers`

- Syntax: `data = client.adjustedNumbers(CompanyKey="NASDAQ_MSFT", PeriodType="annual")`
- Endpoint: `/v1/company/adjusted-numbers`
- Returns: Adjusted numbers payload.
- Notes: Alias endpoint retained for compatibility.

## `segmentsAndKpis`

- Syntax: `data = client.segmentsAndKpis(CompanyKey="NASDAQ_MSFT", PeriodType="annual")`
- Endpoint: `/v2/company/segments-and-kpis`
- Returns: Segments and KPI payload.
- Notes: Uses the v2 endpoint; `legacySegmentsAndKpis` is also available.

## `stockSplits`

- Syntax: `splits = client.stockSplits(CompanyKey="NASDAQ_MSFT")`
- Endpoint: `/v1/company/stock-splits`
- Returns: Stock split history.
- Notes: Returns split records for the selected company.

## `stockPrices`

- Syntax: `prices = client.stockPrices(CompanyKey="NASDAQ_MSFT", StartDate="2026-01-01")`
- Endpoint: `/v2/company/stock-prices`
- Returns: Daily stock price table in auto mode.
- Notes: Use `Latest=true` for only the latest price.

## `intradayStockPrices`

- Syntax: `prices = client.intradayStockPrices(CompanyKey="NASDAQ_MSFT", StartDate="2026-01-01")`
- Endpoint: `/v1/company/stock-prices/intraday`
- Returns: Intraday price payload.
- Notes: Use date filters to limit response size.

## `companyFilings`

- Syntax: `filings = client.companyFilings(CompanyKey="NASDAQ_MSFT")`
- Endpoint: `/v2/company/filings`
- Returns: Filings table in auto mode.
- Notes: Rows may have optional fields; auto mode handles missing values as empty cells.

## `filingImage`

- Syntax: `[bytes, metadata] = client.filingImage(filingId, 1, CompanyKey="NASDAQ_MSFT")`
- Endpoint: `/v1/filing/{filingId}/page/{pageNumber}/image`
- Returns: Image bytes and metadata.
- Notes: Use `OutputFile` to persist the image.

## `filingPdf`

- Syntax: `[bytes, metadata] = client.filingPdf(filingId, CompanyKey="NASDAQ_MSFT")`
- Endpoint: `/v1/filing/{filingId}/pdf`
- Returns: PDF bytes and metadata.
- Notes: Use `OutputFile` to persist the PDF.

## `earningsCalendar`

- Syntax: `events = client.earningsCalendar(Tickers="MSFT", PageSize=25)`
- Endpoint: `/v1/calendar/earnings`
- Returns: Earnings events table in auto mode.
- Notes: Supports ticker lists, date filters, paging, sort, and importance.

## `earningsSummary`

- Syntax: `summary = client.earningsSummary(CompanyKey="NASDAQ_MSFT")`
- Endpoint: `/v1/company/earnings-summary`
- Returns: Latest earnings summary payload.
- Notes: Returns the latest company earnings summary.
